# Use docker image with haskell preinstalled for running jobs
image: mfournial/waskell-env

stages: 
  - build
  - test

build:
  only:
    - master
  script:
    - stack build
  artifacts:
    paths:
      - "src-exe/compile"

test:
  script:
      - stack test --coverage

